{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug CLI",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cli/magi/",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}