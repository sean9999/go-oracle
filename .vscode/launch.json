{
    "version": "0.2.0",
    "configurations": [

        {
            "name": "Debug CLI",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/goracle/",
            //"args": ["encrypt", "--format=pem", "--", "<", "${workspaceFolder}/go.mod"],
            "args": ["info"],
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}
