{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug CLI",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cli/magi/main.go",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}